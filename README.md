Simple Agile tracker. Make sure `node` and `npm` are installed and available in
your **PATH**.

To install application dependencies, type:

    $ npm install -d

To start server on port 3000, type:

    $ node app.js
